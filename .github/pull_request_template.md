## What and why

<!-- The problem, and how this change solves it. Link the issue: Fixes #123 -->

## How it was tested

<!-- Unit tests added, `npm run smoke`, and the manual checks from docs/TESTING.md that apply. -->

## Checklist

- [ ] `npm test` passes
- [ ] Version raised in `package.json`, with a `CHANGELOG.md` entry
- [ ] New strings go through `vscode.l10n.t()`, with `npm run l10n` run and pt-BR translations added
- [ ] README updated if behavior visible to users changed
- [ ] Nothing runs as root or elevated without the user's consent
